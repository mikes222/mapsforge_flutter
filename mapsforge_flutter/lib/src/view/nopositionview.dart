import 'package:flutter/widgets.dart';

class NoPositionView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: const Text("No Position"),
    );
  }
}
