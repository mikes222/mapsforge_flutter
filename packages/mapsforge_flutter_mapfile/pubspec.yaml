name: mapsforge_flutter_mapfile
resolution: workspace
description: A flutter mapsforge library for reading and writing mapfiles.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.9.0

# Add regular dependencies here.
dependencies:

  collection: ^1.19.1

  ecache: ^2.0.13

  logging: ^1.3.0

  # Workspace
  mapsforge_flutter_core: ^1.0.0

dev_dependencies:

  lints: ^6.0.0

  test: ^1.26.2
