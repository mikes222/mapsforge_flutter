abstract class Instructions {
  bool isEmpty();

  /// Returns true if there are subrules.
  bool hasInstructionsNodes();

  /// Returns true if there are subrules.
  bool hasInstructionsOpenWays();

  /// Returns true if there are subrules.
  bool hasInstructionsClosedWays();
}
